/**
 * CustomizerPanel.jsx
 * Placeholder component for future extraction of the customizer panel
 * from the main DemoApp. Currently, the customizer panel is inline
 * within the DemoApp to preserve existing logic 1:1.
 *
 * This file exists to satisfy the folder structure and can be
 * used for modularization in a future refactor.
 */

import React from "react";

const CustomizerPanel = ({ children }) => {
  return <>{children}</>;
};

export default CustomizerPanel;
