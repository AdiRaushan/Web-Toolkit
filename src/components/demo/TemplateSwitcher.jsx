/**
 * TemplateSwitcher.jsx
 * Placeholder component for future extraction of the template/preset switcher
 * from the main DemoApp. Currently, template switching is inline
 * within the DemoApp to preserve existing logic 1:1.
 *
 * This file exists to satisfy the folder structure and can be
 * used for modularization in a future refactor.
 */

import React from "react";

const TemplateSwitcher = ({ children }) => {
  return <>{children}</>;
};

export default TemplateSwitcher;
