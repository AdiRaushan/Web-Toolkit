/**
 * HeroPreview.jsx
 * Placeholder component for future extraction of the hero preview
 * from the main DemoApp. Currently, hero rendering is inline
 * within the DemoApp to preserve existing logic 1:1.
 *
 * This file exists to satisfy the folder structure and can be
 * used for modularization in a future refactor.
 */

import React from "react";

const HeroPreview = ({ children }) => {
  return <>{children}</>;
};

export default HeroPreview;
